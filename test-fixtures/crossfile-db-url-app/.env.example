# Shared configuration for the orders stack.
# The platform injects these same keys into every container (see deploy/platform.yaml).
DATABASE_URL=postgres://orders-db.internal:5432/orders?sslmode=require
ANALYTICS_KEY=ak_live_7f3c9d21b8e4
LOG_LEVEL=info
PORT=3000
