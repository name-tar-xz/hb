# The value every service in this repo is meant to use.
DATABASE_URL=postgres://localhost:5432/app?sslmode=require
# Provisioned by the platform team; the service degrades gracefully without it.
ANALYTICS_KEY=<REPLACE_ME>
